# SQLite database file (relative to project root)
DATABASE_URL="file:./dev.db"

# SMTP — used to email the host when a visitor checks in.
# Leave SMTP_HOST empty to skip sending email (check-ins still save).
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_USER=bb651ae848311d
SMTP_PASSWORD=9a7ee5b4512ba7
SMTP_FROM="Visitor Register <noreply@cyber1systemsnetwork.com>"
